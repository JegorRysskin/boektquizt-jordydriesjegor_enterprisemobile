<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:behavior="clr-namespace:BoektQuiz.Behavior;assembly=BoektQuiz"
             mc:Ignorable="d"
             x:Class="BoektQuiz.Views.ItemDetailPage"
             Title="{Binding Title}">

    <StackLayout Spacing="20" Padding="15">
        <Label Text="{Binding Item.Text}" d:Text="Item name" FontSize="Medium"/>
        <Label Text="Antwoord:" FontSize="Medium" />
        <Entry Text="{Binding Item.Answer}" Placeholder="Vul hier je antwoord in">
            <Entry.Behaviors>
                <behavior:EntryValidationBehavior/>
            </Entry.Behaviors>
        </Entry>
        <Button Text="Antwoord Verzenden" Command="{Binding SendAnswerCommand}" />
    </StackLayout>
    
</ContentPage>